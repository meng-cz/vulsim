# module.serv.rename - 重命名模块的服务端口

## 概述

`module.serv.rename` 用于重命名模块中的服务端口。

## 参数

- name — 要操作的模块名称，字符串类型。
- old_port — 当前端口名称，字符串类型。
- new_port — 新的端口名称，字符串类型。
- update_connections — （可选）设置为 "true" 时自动更新所有连接，布尔/字符串类型。

## 结果

无（成功返回空结果）。

## 错误码
