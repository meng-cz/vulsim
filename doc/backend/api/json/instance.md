# VulInstance 序列化 JSON 格式

用于 `module.instance.*` 操作的 VulInstance 对象的 JSON 序列化格式说明。

```json
{
  "module_name": "...",
  "comment": "...",
  "local_config_overrides": {
     "config1": "value1",
     "config2": "value2"
  }
}
```
