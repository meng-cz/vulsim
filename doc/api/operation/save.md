# save - 将当前打开的项目保存到磁盘

## 概述

save 命令将当前打开的项目写回磁盘，包括模块文件、配置库和 bundle 库。若没有打开项目则返回错误。保存过程中会将修改的数据写入临时目录，完成后替换到项目目录。

## 参数

无

## 结果

无

## 错误码

可能的错误码包括：EOPSaveNotOpened（无打开项目）、EOPSaveFileFailed（写入或最终化保存过程失败）。
