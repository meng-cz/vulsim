# module.serv.update - 更新模块中的服务端口定义

## 概述

`module.serv.update` 用于更新模块中已存在服务端口的定义。

## 参数

- name — 要操作的模块名称，字符串类型。
- port — 要更新的端口名称，字符串类型。
- definition — 端口的新的 JSON 定义，字符串类型。
- force — （可选）布尔类型，是否强制更新以有连接的端口。

## 结果

无（成功返回空结果）。

## 错误码
